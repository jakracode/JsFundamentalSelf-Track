const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

    // At the end of element (pop method)
    const last = arr.pop();

    // At the beginning of element (shift method)
    const first = arr.shift();

    // At the middle of element 
    const mid = arr.splice(2, 1); // Delete at the start of index 2 for 1 element
