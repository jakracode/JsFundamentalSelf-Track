let number = [4, 6, 3, 8, 34, 35, 76, 45, 6];

    // Use indexOf
    number.indexOf(6); // Index 1

    // Find the last index of value
    number.lastIndexOf(6); // Index 8

    number.indexOf(5) != 7; // True

    // Check for value
    number.includes(74); // False
